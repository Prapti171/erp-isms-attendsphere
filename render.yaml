services:
  - type: web
    name: attendsphere-erp-isms
    env: node
    buildCommand: npm install && npm run seed
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
